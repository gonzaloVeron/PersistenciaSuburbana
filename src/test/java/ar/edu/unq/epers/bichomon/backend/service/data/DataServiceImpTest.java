package ar.edu.unq.epers.bichomon.backend.service.data;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

import ar.edu.unq.epers.bichomon.backend.service.data.*;
import ar.edu.unq.epers.bichomon.backend.dao.*;

public class DataServiceImpTest {
/*
    private JDBCEspecieDAO dao = new JDBCEspecieDAO();
    private DataService data = new DataServiceImp(dao);

    @Test
    public void verificarDatosIniciales() {
        data.crearSetDatosIniciales();
        assertEquals(dao.recuperarTodos().size(), 2);
    }

    @Test
    public void verificarBorradoDeDatos() {
        dao.deleteAll();
        assertEquals(dao.recuperarTodos().size(), 0);
    }
*/

}